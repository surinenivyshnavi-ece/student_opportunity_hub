import 'package:flutter/material.dart';

class HackathonsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hackathons")),
      body: Center(child: Text("Hackathon Events Coming Soon")),
    );
  }
}